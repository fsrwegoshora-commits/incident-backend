package com.smartincident.incidentbackend.enums;

public enum AgencyRole {
    LEAD,      // Primary agency (usually POLICE)
    SUPPORT,   // Supporting agency
    STANDBY    // On standby if needed
}
