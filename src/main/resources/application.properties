spring.application.name=incident-backend

# JWT Configuration — change this secret in production (min 32 chars)
jwt.secret=sm4rt1nc1d3nt-s3cr3t-k3y-ch4ng3-1n-pr0d-2026
jwt.expiration.access=86400000
jwt.expiration.refresh=604800000

# OTP Configuration
app.otp.expose-in-response=true
app.otp.rate-limit-seconds=60
spring.datasource.url=jdbc:postgresql://localhost:5432/incidentDb
spring.datasource.username=postgres
spring.datasource.password=salap3938
logging.level.org.springframework=DEBUG
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# GraphQL SPQR disabled — using REST API
spring.autoconfigure.exclude=io.leangen.graphql.spqr.spring.autoconfigure.BaseAutoConfiguration,io.leangen.graphql.spqr.spring.autoconfigure.WebSocketAutoConfiguration,io.leangen.graphql.spqr.spring.autoconfigure.MvcAutoConfiguration

app.seed.enabled=true

# === MEDIA UPLOAD CONFIG ===
app.upload.dir=uploads

# Temporarily disable push notifications until Firebase is fixed
app.notification.push.enabled=true

# Firebase Configuration
firebase.config.path=firebase-service-account.json

# Enable detailed logging
logging.level.com.smartincident.incidentbackend.config.FirebaseConfig=DEBUG
logging.level.com.smartincident.incidentbackend.notification.service=DEBUG